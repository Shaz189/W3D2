<h1>all the cats</h1>

<ul>
  <% @cats.each do |cat| %>
  <li>
    <a href=" <%= cat_url(cat.id) %> "> <%= "#{cat.name}" %> </a>
  </li>
  <% end %>
</ul>

<a href=" <%= new_cat_url %> ">Make a new cat</a>
